<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.JustEatBurguer_v2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.JustEatBurguer_v2" parent="Base.Theme.JustEatBurguer_v2" />

    <!-- Tema principal do app -->
    <style name="Theme.JustEatBurguer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Cor da barra de status -->
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <!-- Tema da SplashScreen -->
    <style name="Theme.SplashScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>