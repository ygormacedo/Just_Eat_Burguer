package devandroid.macedo.justeatburguer_v2.repository;

import devandroid.macedo.justeatburguer_v2.model.User;

public class UserRepository {
    public void saveUser(User user) {
        // Simula o salvamento dos dados do usuário (pode ser substituído por um banco de dados ou API).
    }
}